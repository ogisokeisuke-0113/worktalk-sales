# 小木曽さんへ：お願いしたいこと（所要5分）

2026-09-10、Sales Board で **架電記録が保存されないことがある** 不具合を修正し、本番へ反映しました。
ただ、いま **1点だけ危ない状態** なので、それを解消させてください。

---

## いまの状態

稼働中のコードのソースが GitHub にありません。

- `main` ブランチは **8月4日**で止まっています
- `gh-pages` にはビルド済みのファイル（`assets/index-*.js`）しか入っていません
- 9/8〜9/10 に入れていただいた修正（担当者フィルタ、テストユーザー除去、callHistoryのマージ）の**ソースはどこにも残っていません**

そのため今回の修正は、やむを得ず**配信中のバンドルへ直接パッチを当てる**形で入れました。

**このままだと、小木曽さんが手元のソースから `npm run build` して deploy した瞬間に、修正が消えて元の不具合に戻ります。**

---

## お願い① 手元のソースを GitHub に上げてください（これが一番大事）

作業フォルダで、これだけ実行してください。

```bash
git checkout -b restore/source-2026-09-10
git add -A
git commit -m "restore: 稼働中のソース一式"
git push -u origin restore/source-2026-09-10
```

`.env.local` は `.gitignore` に入っているので、Supabase のキーが混ざる心配はありません。
（もし他に手元だけにある設定ファイルがあれば、中身は伏せたまま「こういうファイルがある」とだけ教えてください）

**これをしていただければ、あとはこちらで進めます。**
小木曽さんの側で修正コードを書き写す作業は不要です。

---

## お願い② これ以降は deploy しないでください（一時的に）

お願い①のあと、こちらで

1. 上げていただいたソースへ今回の修正を反映（`SOURCE-PATCH.md` の内容）
2. `main` へマージ
3. 自動配信の有効化（下記）

まで進めます。それが終わるまでは、手元からの `gh-pages` への push を止めておいてください。
終わったらご連絡します。

---

## そのあとの運用（自動配信）

`tools/github-actions-deploy.yml` を用意してあります（`.github/workflows/deploy.yml` へ移動して有効化します）。有効になると、

```
main に push  →  GitHub 側で自動ビルド  →  gh-pages へ自動配信
```

となり、**手元で build して dist を手で push する作業がなくなります。**

これで解決すること:

| いまの困りごと | 自動配信後 |
|---|---|
| 配信されているコードのソースが分からない | 常に `main` のあるコミットと一致する |
| 2人で触ると、後から deploy した方の内容で上書きされる | `main` で合流するので上書きが起きない |
| 修正が入っていないビルドを誤って配信してしまう | ビルド結果を検査して、入っていなければ配信を止める |
| 手元の環境差でビルド結果が変わる | GitHub 上の同じ環境でビルドされる |

**小木曽さんの作業手順はシンプルになります。**

```bash
git pull            # 最新を取る
# ...修正する...
git push            # これだけ。deploy 作業は不要
```

---

## 今回直した内容（参考）

本番DBを調べたところ、ステータスが「架電済／アポ確定」なのに架電履歴が空の行が **447件**（該当行の17.6%）ありました。原因は4つ重なっていました。

| | 不具合 | 何が起きるか |
|---|---|---|
| P1 | デバウンスの比較基準が送信前に進む | 1.5秒以内に次の操作をすると、先の記録が差分から消えて二度と送られない |
| P2 | 起動時に全件 upsert | localStorage が6.14MBで上限超過 → 毎起動で9,647行を丸ごと上書き |
| P3 | `.in()` が600件超で400、かつ `error` 未確認 | マージが無言で失敗し、素の上書きに戻る |
| P4 | 失敗が `console.warn` のみ | 成功と失敗が見分けられず、再試行もされない |
| P5 | 離脱時のフラッシュが無い | 記録直後にタブを閉じると消える |

詳細と修正コードは **`SOURCE-PATCH.md`** に書いてあります。
検証用の実ブラウザテストは `tools/e2e.js` です（Supabase は全てモックするので本番DBには触れません）。

```bash
node tools/e2e.js dist/assets/index-XXXX.js
SCENARIO=coldstart node tools/e2e.js dist/assets/index-XXXX.js
SCENARIO=failretry node tools/e2e.js dist/assets/index-XXXX.js
SCENARIO=unload    node tools/e2e.js dist/assets/index-XXXX.js
```

`callHistory` のマージを入れていただいた狙いは正しかったのですが、`.in()` の件数上限に当たって
無効化されていました。そこも含めて直してあります。

---

## このあと予定しているもの

1. `supabase/migrations/001_call_logs.sql` — 架電履歴を INSERT だけの追記専用テーブルへ切り出す（行が競合しなくなるので、上書きも取りこぼしも原理的に起きなくなる）
2. Realtime購読 — 開きっぱなしのタブが古いデータを持ち続ける状態の解消
3. localStorage の全件キャッシュ廃止（6.14MBで上限超過している）
4. RLS の見直し — 現在、認証なしで9,647社の企業名・電話番号・商談メモが読み書きできる状態です

ご不明点あればいつでも聞いてください。
