# 本番の接続先。ここを間違えると全員が別のDBに繋がるため、リポジトリで固定する。
#
# 2026-09-10、8月時点の .env.local が arhgxtdwnsciwmotvokl（空のプロジェクト）を
# 指しており、それでビルドすると全員が空のDBに接続する状態だった。
# 本番は antqewvlzfonsakgndxt。取り違えないようここに明示する。
#
# このキーはビルド結果のJSに埋め込まれて公開される性質のもので、
# 秘密情報ではない（gh-pages 上の配信ファイルにも既に入っている）。
# 本来の防御は Supabase 側の RLS で行う。現状 RLS が匿名に開いているのは別途対応する。
VITE_SUPABASE_URL=https://antqewvlzfonsakgndxt.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable__37RI5zv54AI023pjvFdbA_AUkoAme2
